<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
  <meta http-equiv="x-ua-compatible" content="ie=edge">
  <title>Simple Transfer Market</title>
  <!-- Font Awesome -->
  <link rel="stylesheet" href="https://use.fontawesome.com/releases/v5.8.2/css/all.css">
  <!-- Bootstrap core CSS -->
  <link href="../../dist/css/bootstrap.min.css" rel="stylesheet">
  <%# <link href="css/bootstrap.min.css" rel="stylesheet"> %>
</head>

<body>
 <section class="jumbotron text-center">
        <div class="container">
          <h1 class="jumbotron-heading">Simple Transfer Market</h1>
          <p class="lead text-muted">To get the full functionality of this website, please Sign up as an agent, you can add, edit and delete players with their market value.</p>
          <p>
            <a class="btn btn-primary my-2" href:<%= link_to "Sign Up Here", new_user_path %> </a>
            <%# <a href="#" class="btn btn-secondary my-2">Secondary action</a> %>
          </p>
        </div>
      </section>
     <% @stories.each do |story| %>
      <div class="row">
        <div class="col-sm">
          <div class="card-deck">
            <div class="card">
             <% if story["urlToImage"].present?  %>
                 <img class="card-img-top"  <%= image_tag story["urlToImage"]%>
                   <div class="card-body">
                    <h5 class="card-title"><%= story['title'] %> </h5>
                      <a class="btn btn-primary" id='story' href="<%= story['url'] %>">Read The Story</a>
                    </div>
                 <div class="card-footer">
              <small class="text-muted"><%= Date.parse(story['publishedAt'])%></small>
            </div>
        </div>
      </div>
    </div>
  </div>

  
  <% end %>
  <% end %>

  <!-- SCRIPTS -->
  <!-- JQuery -->
</body>

</html>